spring.application.name=jeans-inventory

# =========================================================
# PostgreSQL - Neon
# =========================================================
spring.datasource.url=jdbc:postgresql://ep-round-poetry-ay7djmj0-pooler.c-5.us-east-2.aws.neon.tech/new_db_839942d2?sslmode=require&channelBinding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_C9RGjakO7bgF

# =========================================================
# JPA / Hibernate
# =========================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# =========================================================
# Server
# =========================================================
server.port=${PORT:8080}

# =========================================================
# Admin Login
# =========================================================
spring.security.user.name=${APP_ADMIN_USERNAME:admin}
spring.security.user.password=${APP_ADMIN_PASSWORD:change-this-before-production}
spring.security.user.roles=ADMIN