package com.jeans_inventory.repository;

import com.jeans_inventory.entity.AlterRecord;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AlterRecordRepository extends JpaRepository<AlterRecord, Long> {
}
